'use strict';

const getMediator = require('../dev/getMediator');

describe('verifying if mediator is perpendicular to line', () => {
  it('Something to test', () => {
    expect(spectraTest()).toBe(true);
  });
});
